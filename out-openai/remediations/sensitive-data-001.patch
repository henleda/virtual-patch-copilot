--- me/route.js
+++ me/route.js
@@ -7,7 +7,10 @@
   if (!customer) {
     return NextResponse.json({ ok: false, error: 'Not signed in' }, { status: 401 });
   }
-  return NextResponse.json({ ok: true, customer });
+  const { id, name } = customer; // Only include non-sensitive fields
+  return NextResponse.json({ ok: true, customer: { id, name } });
 }
