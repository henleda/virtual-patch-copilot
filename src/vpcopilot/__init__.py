"""virtual-patch-copilot: agent pipeline that finds vulnerabilities, triages each to
the right F5 Distributed Cloud control (or to code), generates the virtual patch, and
drafts the real code fix. Model-independent."""

__version__ = "0.1.0"
