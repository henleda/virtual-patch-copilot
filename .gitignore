# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/

# Secrets — never commit
.env
.env.*
!.env.example
env-for-new.txt

# Pipeline output (findings, generated policies, PR drafts) — any out* dir
out*/
# ...except the curated offline demo dataset (C6), which is committed on purpose
!demo/out/
!demo/out/**

# Global live-band-aid inventory + reconcile pass-lock — runtime state, like a session ledger.
inventory.json
reconcile.lock

# OS / tooling cruft — tracked by mistake historically, untracked in this commit
.DS_Store
.coverage
coverage.xml
.ruff_cache/

# Local agent / orchestration runtime dirs
.claude-flow/
.swarm/
